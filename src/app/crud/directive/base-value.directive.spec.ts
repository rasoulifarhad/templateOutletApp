import { BaseValueDirective } from './base-value.directive';

describe('BaseValueDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseValueDirective();
    expect(directive).toBeTruthy();
  });
});
