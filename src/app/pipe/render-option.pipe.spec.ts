import { RenderOptionPipe } from './render-option.pipe';

describe('RenderOptionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RenderOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
